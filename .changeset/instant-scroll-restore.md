---
"tg-mini-app-uikit": patch
---

Modal scroll lock now restores the page position instantly on release — host pages with `scroll-behavior: smooth` no longer animate from the top after closing a sheet, dialog or image viewer.
