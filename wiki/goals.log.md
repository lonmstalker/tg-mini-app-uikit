# Goal log (append-only)


One line per goal EVENT. A status change is a NEW line, never an edit of an old
one. Newest at the bottom. `DONE` lines must cite verification evidence (tests
passing, build green, reviewers' claims independently re-verified). Timestamps are
ISO-8601 UTC. Statuses: `OPEN | IN_PROGRESS | BLOCKED | DONE | REVERTED`. Format:

    <ISO-8601 UTC> | <goal-id> | <status> | <goal statement> | evidence: <…>

The goal-id is hierarchical: `G1` is a goal, `G1.M0` a milestone under it. See the
reviewer + verification discipline in [[testing-and-review]] and the rules in
`.claude/agents.md`.

---

- 2026-06-14T21:06Z | G1 | OPEN | Ship the Trailhead flagship demo per trailhead-demo.plan.md (milestones M0–M6), satisfying: no demo Storybook, full ru/en, TDD unit+e2e, per-checkpoint reviewer verification, visual+contrast checks. | evidence: ExecPlan at repo root; requirements set by product owner 2026-06-14.
- 2026-06-14T21:06Z | G1.M0 | OPEN | App shell + per-tab TKNavStack + injected mock + MOCK badge; swipe-back and back-priority proven on placeholder panels. | evidence: gate is shell.spec.ts (red before M0, green after).
- 2026-06-14T21:16Z | G1.plan | DONE | Authored the ExecPlan + Karpathy wiki + .claude/agents.md; folded in the five owner requirements (no demo Storybook, full ru/en, TDD unit+e2e, per-checkpoint reviewer verification, visual+contrast checks). | evidence: a reviewer agent claimed all 5 COVERED + flagged stale present-tense `plans.md` references; independently re-verified via `git status` (`D plans.md`) and `git show HEAD:plans.md` (audit still in history, commit 6aa57cb) and corrected every reference to past/archived; canon order, zero-tables, wiki-link resolution, and tooling-availability checks all green.
- 2026-06-14T21:45Z | G1.M0 | IN_PROGRESS | App shell + per-tab TKNavStack + injected mock + MOCK badge; swipe-back and back-priority on placeholder panels. | evidence: confirmed baseline `npm run typecheck` exit 0; verified kit APIs (TKTelegramProvider webApp prop, TKNavStack data-tk-nav-panel, useVerticalSwipes().disable, getTelegramWebApp, enLocale/ruLocale, TKLocaleProvider) against source before scaffolding examples/trailhead.
- 2026-06-14T22:12Z | G1.M1 | IN_PROGRESS | Mock-data layer (experiences/bookings/sessions/people/messages) with tunable latency + injectable failure; above-stack store (context+reducer) for bookings/cart/streak/themePrefs/onboarding/wallet; mirror to Cloud/Secure/Device storage; rehydrate on startup; first-run welcome gated by persisted onboarding flag. | evidence: gate is persistence.spec.ts (onboarding does not replay after reload) + unit tests (reducer, mockApi delay/failure, persistence round-trip, Trail Pass discount math).
- 2026-06-15T00:20Z | G1.M6 | IN_PROGRESS | Finish: onboarding coach marks + add-to-home, failed-checkout error/retry, a11y + reduced-motion pass, README recording script, prod-parity (dist build) + /testing fallback. | evidence: gates are onboarding.spec (walk 3 marks, no replay), a11y.spec (axe + reduced-motion), booking failpay, TRAILHEAD_USE_DIST=1 build.
- 2026-06-15T00:55Z | G1.M6 | DONE | Finish milestone: first-run onboarding (welcome → 3 TKOnboardingTooltip coach marks anchored to feed/tabbar/Train-tab → useHomeScreen add-to-home toast, gated by the persisted flag); failed-checkout error/retry (?failpay=1); axe + prefers-reduced-motion e2e; README recording script; prod-parity dist build with the /testing mock kept as a documented source alias. | evidence: GATES — `npm run typecheck` (kit) OK, `npm run test:unit` (kit) 612/612 OK, `npm run build` (kit) OK, `npm run check:package` OK; `npm run typecheck -w trailhead` OK, `npm run test -w trailhead` 33/33 OK, `npx playwright test --project=trailhead` 26/26 OK (added onboarding tour-walk en+ru, a11y axe + reduced-motion), `npm run build -w trailhead` OK, `TRAILHEAD_USE_DIST=1 npm run build -w trailhead` OK (dist build serves; bundle references kit dist symbols). VISUAL — onboarding coach marks {en,ru}, AA accent feed, add-to-home toast. REVIEWERS — code-reviewer ("correct and ship-ready as code") + adversarial skeptic ("M6 does NOT hold"). Re-verified + acted on EVERY material finding: (1) HIGH axe color-contrast blanket-disabled hiding real white-on-accent failures (3.31) — FIXED HONESTLY: darkened default accent #3390ec→#1c6fd3 and all Platform Lab presets to clear AA 4.5:1, re-enabled color-contrast in the axe scan (only the kit TKXPHeader translucent-brand badge excluded by element). (2) MEDIUM no test walked the 3 coach marks — FIXED (onboarding.spec walks all three + add-to-home + no-replay, en+ru). (3) MEDIUM `/testing` export dropped without the Decision-Log record its fallback requires — FIXED (dated M6 Decision-Log entry + two stale Surprises notes updated + Step-5 "Expected" reworded). (4) HIGH no goals.log M6 entry / box unchecked — FIXED (this entry + checklist). (5) MINOR card a11y (nested-interactive) — already FIXED earlier (overlay-button); confirmed axe-clean. CARRYOVER (recorded): promoting the mock to a real `tg-mini-app-uikit/testing` package export is deferred to a future kit touch (moving it into src/ risks the kit's 90% coverage gate + needs a multi-entry lib build); prod-parity proves the production exports (`.`,`./style.css`) resolve from dist.
- 2026-06-15T00:20Z | G1.M5 | DONE | Thickened Train (TKXPHeader, TKRing streak Day 5/7, TKStatTile grid, TKProgress, TKLeaderboard of same-trip friends, sessions by week → session-detail push with TKSteps+TKAccordion) and Guide (TKVirtualList directory + TKAvatarStack, useLongPress→TKActionSheet, profile → TKMessages DM thread with status ticks + TKWriteBar). All five tabs are now real; PlaceholderStack deleted. | evidence: GATES — kit+demo typecheck OK; `npm run test -w trailhead` 33/33 OK; `npx playwright test --project=trailhead` 21/21 OK (train push+swipe-back en+ru, guide directory→profile→DM with delivered/read status en+ru, long-press action sheet, shell spine retargeted to the real 3-deep Guide stack); `npm run build -w trailhead` OK. VISUAL — Train dashboard (XP header, 5/7 ring, sparkline stats), Guide directory (avatar stack + badges), DM thread (bubbles with time + ✓✓ status, write bar respecting safe area) verified in preview, en+ru. REVIEWERS — code-reviewer + adversarial skeptic; functional claims all survived (push+swipe-back, directory→profile→DM both langs, long-press sheet, full i18n parity, 5 real independent stacks). Acted on findings: (1) MEDIUM touch long-press+tap double-fire (sheet opened then click pushed profile) → FIXED (suppress ref in GuideRow). (2) MEDIUM hardcoded "now" timestamp (English in both langs) → FIXED (chat.now key, en/ru). (3) LOW leaderboard "You You" → FIXED (you-row uses the user's name). (4) LOW sendMessage duplicate React keys for equal-length messages → FIXED (monotonic id). (5) LOW stale tabs.ts "four-tab cut" comment → REMOVED. NOTE (skeptic process): the demo workspace is untracked in git — committing is the owner's call (not part of milestone code); milestone outcomes are recorded here per the reviewer-verification discipline.
- 2026-06-14T23:58Z | G1.M4 | DONE | Profile: PIN-gated TON wallet connect → Trail Pass cell → 15% discount wired causally into the M2 checkout; settings (closing-confirmation toggle, TKDialog disconnect); Platform Lab live re-theming (accent/radius/type/motion, light↔dark via mock setColorScheme, device cutouts, RTL, language) all persisted via DeviceStorage. | evidence: GATES — kit+demo typecheck OK; `npm run test -w trailhead` 33/33 OK; `npx playwright test --project=trailhead` 16/16 OK (wallet en+ru lowers Stars 450→382 in summary AND pay button; radius slider + dark toggle re-skin live; dark+radius persist across reload); `npm run build -w trailhead` OK. VISUAL — Profile, wallet connected (Tonkeeper + address + Trail Pass), Platform Lab live (green accent + dark + cutouts safe-top 59px) verified in preview; accent segmented aria-pressed correct. REVIEWERS — code-reviewer + adversarial skeptic both "M4 holds"; skeptic refuted every attack live (causal 450→382 both langs + pay button; live radius/dark; persist incl. accent/RTL/cutouts/lang; ?lang override intact; no PIN bypass; RTL dir=rtl; cutouts real insets). Re-verified + acted on low-severity findings: dead i18n keys (lab.preview, wallet.connectedLabel/connecting, settings.language, lang.*, placeholder.comingSoon) → REMOVED from both dicts; vestigial motionSpeed knob (no control drove it) → DROPPED from ThemePrefs/AppFrame/reset; uncleaned PinGate error timeout → FIXED (ref + cleanup); lab-persist e2e strengthened to also assert radius survives. PLAN-TEXT imprecisions recorded (density knob has no kit prop → mapped to fontSize; RTL is via document.dir not TKLocaleProvider).
- 2026-06-14T23:35Z | G1.M3 | DONE | Trips tab (booking list + status badges, pull-to-refresh, swipe Reschedule/Cancel + Undo, empty state) + the signature QR→biometric→location check-in. The full signature chain (book → pay → arrive → persist) now runs end to end. | evidence: GATES — kit+demo typecheck OK; `npm run test -w trailhead` 33/33 OK (added useCheckIn chain + idempotency unit tests); `npx playwright test --project=trailhead` 12/12 OK (checkin en+ru flips to "Checked in" and survives reload; swipe→Cancel→Undo restores; pull-to-refresh keeps the app open; plus M0-M2 regression); `npm run build -w trailhead` OK. SIGNATURE CHAIN verified live (preview): Trips seed booking → Check in → QR (useQrScanner) → biometric (useBiometrics) → location (useLocation) → SET_BOOKING_STATUS checkedIn → persisted to cloud (survives reload) → card badge "Checked in". VISUAL — Trips list, check-in progress + done across en/ru/light; booking summary text stacks correctly; "Back to my trips" CTA. REVIEWERS — code-reviewer + adversarial skeptic both returned "M3 holds"; skeptic could not refute (live-confirmed checkedIn persisted in th_bookings, byte-identical Undo restore, idempotent via latch). Re-verified + acted on findings: (1) MEDIUM full-swipe fired Reschedule not Cancel (TKSwipeCell defaults fullSwipe) → FIXED (fullSwipe={false}; deliberate tap on revealed rail; e2e hardened to assert Cancel visible). (2) dead i18n key checkin.alreadyTitle → FIXED (wired into the already-checked-in title). (3) unused useCheckIn.reset → REMOVED. (4) skeptic F1 useCheckIn untested → FIXED (unit test: chain order + status flip + idempotency via mock log). KIT NOTE — TKBookingCard action renders a real <button> (a11y OK, contrary to a flagged risk).
- 2026-06-14T22:12Z | G1.M2 | IN_PROGRESS | Discover end-to-end: feed (banner/categories/chips/search/infinite-list/skeletons/error-retry/empty) → detail (gallery/bio/timeline) → 3-panel funnel → snap-point checkout sheet → PIN/biometric → Stars invoice → confetti/toast/closing-confirmation → persisted booking; bilingual mock data. | evidence: gate is booking.spec.ts (signature chain en+ru, swipe-back keeps slot, feed error/retry).
- 2026-06-14T23:13Z | G1.M2 | DONE | Discover end-to-end believable core + the M1-carryover feed error/retry. | evidence: GATES — kit+demo typecheck OK; `npm run test -w trailhead` 31/31 OK; `npx playwright test --project=trailhead` 8/8 OK (booking en+ru full signature chain incl. exactly-one-booking + reload survival, mid-funnel swipe-back keeps slot, feed failure→error/retry→content, shell spine retargeted to Train, persistence); `npm run build -w trailhead` OK. SIGNATURE CHAIN verified live (preview, en+ru): feed → Sunrise Ridge detail → Book 450 Stars → 07:00 (11:00 sold-out disabled) → Continue → summary TKPaymentSummary → Pay → snap-point TKSheet confirm → TKPinInput (set PIN 1234, biometric fast-path) → useInvoice→"paid" → 🎉 TKConfetti + toast + useClosingConfirmation + booking persisted to cloud (survives reload) → View my trips lands on Trips. VISUAL — feed/detail/datetime/summary/checkout/success across {light,dark}×{en,ru}; single-emoji card thumb; no overflow. KIT FINDINGS — #16 (InfiniteList double-fetch) and #14 (PIN last dot) re-verified ALREADY RESOLVED in current kit source (infinite-list.tsx firedRef latch; pin.tsx PIN_FILL_HOLD_MS=180) — no kit change needed; demo adds its own paging guard (reqId+inFlight). REVIEWERS — code-reviewer found HIGH double-book (settle had no sync latch) → FIXED (useRef latch + stable booking id `bk-exp-date-slot`; reducer id de-dupe is now a real 2nd defense; e2e asserts exactly 1 + reload-survives); LOW zombie summary after pay → FIXED (nav.popTo("feed") in finishToTrips); LOW double-emoji card → FIXED (single gradient thumb); render-write cursorRef → FIXED (moved to effect). Adversarial skeptic could NOT refute M2 (retracted a dark-contrast claim as a measurement artifact; real ratios ≥ AA). DEFERRED: axe color-contrast assertion → M6 cross-cutting; lang/colorScheme in themePrefs → M4.
- 2026-06-14T22:33Z | G1.M1 | DONE | Mock-data layer + above-stack store + Telegram-storage persistence + first-run welcome, with one acceptance half (failure-flag error/retry UI) routed to M2 where the feed that surfaces it lives. | evidence: GATES — `npm run typecheck` (kit+demo) OK; `npm run test -w trailhead` 30/30 OK across 6 files (reducer 11, mockApi 8 incl. getPerson failure, pricing 4, persistence 6, StoreProvider StrictMode hydrate→persist round-trip 2, i18n parity 3, minus overlap); `npx playwright test --project=trailhead` 4/4 OK (shell + persistence, en+ru); `npm run build -w trailhead` OK. STORE is above the nav stacks (main.tsx: TKTelegramProvider→MockProvider→StoreProvider→AppFrame→App→per-tab TKNavStack), so a swipe-back/tab-switch cannot discard cart. PERSISTENCE round-trips via the mock's localStorage-backed Cloud/Device/Secure storage (prefixes tg-demo-cloud:/tg-demo-device:/tg-demo-secure:); close/reopen verified headed (preview: dismiss welcome → reload → no replay; th_onboarding="true", seed booking survives) and headless (persistence.spec). VISUAL — welcome {light,dark}×{en,ru}, elevated card, AA-readable. REVIEWERS — code-reviewer ("no correctness bugs") + adversarial skeptic ("M1 does NOT hold" on the error/retry half); both ran the gates green. Re-verified claims: (1) getPerson missing from failure test — FIXED. (2) StoreProvider effect untested — FIXED (store-provider.test.tsx, StrictMode). (3) persistence.ts comment wrong about localStorage fallback + first-write nuance — FIXED. (4) post-reload booking assertion not poll — FIXED. (5) CARRYOVER: failure-flag error/retry UI (plan M1 acceptance L618-619) is delivered in M2 (Discover feed: TKSkeletonList + error/retry + content), not silently skipped — the mockApi failure contract is already met and unit-tested. (6) DEFERRALS recorded: lang/colorScheme not yet in themePrefs (→ M4 Platform Lab; LangProvider already supports controlled lang); mock seed data is English-only and MUST be localized when M2 renders it (zero-hardcoded-strings rule).
- 2026-06-14T22:11Z | G1.M0 | DONE | App shell: `examples/*` workspace registered; Vite+React19+TS `examples/trailhead`; providers wired (TKTelegramProvider+mock → TKProvider(telegram,theme) → LangProvider/TKLocaleProvider → TKToastProvider); typed ru/en dicts (shared `Dict` type, `useT`) with zero hardcoded user-facing strings; five-tab TKTabbar each owning an independent mounted TKNavStack; MOCK badge when getTelegramWebApp()===null; vertical swipes disabled at root. | evidence: GATES — `npm run typecheck` (kit) OK, `npm run typecheck -w trailhead` OK, `npm run test -w trailhead` 3/3 OK (i18n key-parity/empty/placeholder), `npm run build -w trailhead` OK, `npx playwright test --project=trailhead` 2/2 OK (shell spine in en AND ru: 5 tabs+MOCK badge, push to depth 3, real PointerEvent edge-swipe-back pops exactly one, mounted `marks` state survives pop + Trips→Discover round-trip). VISUAL — preview screenshots {light,dark}×{en,ru}; tabbar bottom + safe-area, MOCK badge top-right, no overflow. REVIEWERS — code-reviewer + adversarial skeptic both ran the gates and returned "M0 holds"; material claims re-verified independently: (A) MAJOR `tg-mini-app-uikit/testing` is not yet a real package export so `TRAILHEAD_USE_DIST=1` would not resolve it — CONFIRMED against `packages/uikit/package.json` exports ({".","./style.css"} only); not an M0 blocker (dist mode runs at M6, which adds the real export); recorded as a tracked deferral. (B) MINOR e2e English-only — FIXED: spec parametrized over en+ru via the `?lang=` override. (C) MEDIUM literal "scroll offset unchanged" not auto-asserted — CONFIRMED + RESOLVED: headless Chromium drops `scrollTop` of off-screen panels under both `visibility:hidden` and `display:none`; scroll preservation verified headed in the live preview and guaranteed by the kit mounting lower panels (nav.tsx); e2e asserts the equivalent mounted-state contract via the `marks` counter.
- 2026-06-15T00:55Z | G1 | DONE | Shipped the Trailhead flagship demo per trailhead-demo.plan.md (M0–M6 all DONE), satisfying every owner requirement: no demo Storybook (Storybook stays package-only); full ru/en with zero hardcoded user-facing strings (shared Dict type + key-parity test); TDD unit (33) + e2e (26, en+ru); per-checkpoint reviewer agents whose claims were independently re-verified; visual + contrast checks (axe color-contrast ENABLED via an AA accent). | evidence: signature chain runs end-to-end in a plain browser (book→pay 450 Stars behind PIN→confetti→QR check-in→reload survives), en+ru; kit gates (typecheck, 612 unit, build, check:package) + demo gates (typecheck, 33 unit, 26 e2e, source build, TRAILHEAD_USE_DIST=1 prod-parity build) all green; outcomes recorded in the ExecPlan's Outcomes & Retrospective. Carryover: real `tg-mini-app-uikit/testing` package export (kept a documented source alias).
