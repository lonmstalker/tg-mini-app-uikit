/*
 * Side-effect-only module: executing it defines `window.Telegram.WebApp`
 * (the official Telegram bridge, vendored — see the package README/CHANGELOG
 * for the refresh procedure). Import it FIRST in the app entry:
 *
 *   import "@tg-mini-app/telegram/bridge";
 */
export {};
